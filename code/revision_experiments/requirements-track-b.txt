# Additions on top of NewsReWrite/requirements.txt.
# torch / transformers / pandas / numpy / tqdm / requests / python-dotenv are
# already pulled in by the repo, so they are not re-declared here.
bert-score>=0.3.13
sentence-transformers>=2.7.0
scikit-learn>=1.3
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
anthropic>=0.34  # optional, only if you enable --with-llm-judge
